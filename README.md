# Deadlock-Detection-and-Recovery-System
The Deadlock Detection and Recovery System simulates an operating system environment where multiple processes compete for limited resources. The system detects deadlocks using a Wait-For Graph (WFG) derived from the Resource Allocation Graph (RAG) and demonstrates recovery mechanisms such as process termination and resource preemption.
